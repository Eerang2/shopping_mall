spring.application.name=shopping_mall

spring.config.import=application-private.properties

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/ShoppingMall?serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=dnalgustjr!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true
spring.jpa.format-sql=true


jwt.secret.key=${JWT_SECRET_KEY}

# ?? ???
id.prefix=${ID_PREFIX}

# ?? ???? ??? ?? (???? ? ??)
id.characters=${ID_CHARACTER}

# ?? ???? ??
id.length=${ID_LENGTH}


spring.mvc.contentnegotiation.favor-parameter=true
spring.mvc.contentnegotiation.parameter-name=mediaType
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

file.image.upload-dir=${IMAGE_DIR}
portone.api.key=${IMPORT_API_KEY}
portone.api.secret=${IMPORT_SECRET_KEY}


